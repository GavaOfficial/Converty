# Converty Backend - Environment Configuration
# Copy this file to .env and fill in the values

# ===========================================
# DATABASE
# ===========================================
# SQLite database URL (default: sqlite:converty.db?mode=rwc)
DATABASE_URL=sqlite:converty.db?mode=rwc

# ===========================================
# SERVER
# ===========================================
# Host to bind the server (default: 127.0.0.1)
CONVERTY_HOST=127.0.0.1

# Port to bind the server (default: 4000)
CONVERTY_PORT=4000

# ===========================================
# FILE HANDLING
# ===========================================
# Maximum file size in MB (default: 50)
CONVERTY_MAX_FILE_SIZE_MB=50

# Temporary directory for file processing (default: system temp dir)
# CONVERTY_TEMP_DIR=/tmp/converty

# ===========================================
# GOOGLE OAUTH (Required for authentication)
# ===========================================
# Get these from Google Cloud Console:
# https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ===========================================
# CORS / FRONTEND
# ===========================================
# Frontend URL for CORS and OAuth redirects (default: http://localhost:3000)
FRONTEND_URL=http://localhost:3000
